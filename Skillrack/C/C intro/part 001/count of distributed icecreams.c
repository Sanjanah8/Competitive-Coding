/*Fareen purchased X icecream(s) for a group of Y person(s). The program must calculate the number of icecream(s) given to each person in the group.*/
#include<stdio.h>
#include<stdlib.h>

int main()
{
int x,y;
scanf("%d %d",&x,&y);
printf("%d",x/y);
return 0;
}
